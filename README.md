# Tempo-64X
Tempo is a language that can connect with Asm Nasm or Any Links Its like Assembly but a little bit hard than Asm Asm has jmp but tempo has jns amd Asm has cmp Compare but tempo has clp tempo has same registers tha Asm has Like eax ebx ecx edx esi edi rax rdi rsi rdi rdp rip  , And we make our coding Faster soon.
